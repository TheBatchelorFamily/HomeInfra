---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: portfolio
  namespace: argocd

  annotations:
    # 1. Define the image to monitor (alias = portfolio)
    argocd-image-updater.argoproj.io/image-list: portfolio=fiercekitti/portfolio_website
    
    # 2. Use the 'latest' strategy to grab the newest tag by creation date
    argocd-image-updater.argoproj.io/portfolio.update-strategy: latest
    
    # 3. Only allow tags that follow your beta naming convention
    argocd-image-updater.argoproj.io/portfolio.allow-tags: regexp:^.*-beta.*$

    # 4. Commit the new tag back to your HomeInfra repo automatically
    argocd-image-updater.argoproj.io/write-back-method: git

spec:
  project: apps

  source:
    repoURL: https://github.com/TheBatchelorFamily/HomeInfra.git
    targetRevision: main
    path: gitops/apps/portfolio/manifests

  destination:
    server: https://kubernetes.default.svc
    namespace: portfolio

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
